# HTML Test

A minimal static HTML page used to verify that deployment and hosting are working correctly.

## How it works

- Single `index.html` file with inline CSS and JavaScript
- Displays a status card with the current timestamp
- No build step required
