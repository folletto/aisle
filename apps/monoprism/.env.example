# Google API key for Drive API — enables public folder access without login.
# Copy this file to .env.local for local development (never commit .env.local).
# For Netlify, set VITE_GOOGLE_API_KEY in Site settings → Environment variables.
VITE_GOOGLE_API_KEY=
